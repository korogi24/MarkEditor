<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="MarkEditor.ToolBar"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MarkEditor"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="using:CommunityToolkit.WinUI"
    mc:Ignorable="d">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="ms-appx:///CommunityToolkit.WinUI.Controls.Ribbon/RibbonStyle.xaml" />
            </ResourceDictionary.MergedDictionaries>

            <Style BasedOn="{StaticResource DefaultAppBarButtonStyle}" TargetType="AppBarButton">
                <Setter Property="LabelPosition" Value="Collapsed" />
                <Setter Property="Width" Value="48" />
                <Setter Property="Height" Value="48" />
            </Style>

            <Style BasedOn="{StaticResource DefaultAppBarToggleButtonStyle}" TargetType="AppBarToggleButton">
                <Setter Property="LabelPosition" Value="Collapsed" />
                <Setter Property="Width" Value="48" />
                <Setter Property="Height" Value="48" />
            </Style>

            <Style TargetType="Grid">
                <Setter Property="ColumnSpacing" Value="1" />
                <Setter Property="RowSpacing" Value="1" />
            </Style>
        </ResourceDictionary>
    </UserControl.Resources>

    <StackPanel Spacing="4">
        <controls:Ribbon>
            <controls:RibbonGroup Label="元に戻す"
                                  Style="{StaticResource RibbonLeftGroupStyle}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <AppBarButton Icon="Undo" />
                    <AppBarButton Grid.Row="1" Icon="Redo" />
                </Grid>
            </controls:RibbonGroup>
            
            <controls:RibbonCollapsibleGroup Label="クリップボード"
                                             IconSource="{ui:SymbolIconSource Symbol=Paste}"
                                             Style="{StaticResource RibbonLeftCollapsibleGroupStyle}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <AppBarButton Icon="Paste" Label="貼り付け" />
                    <AppBarButton Grid.Column="1" Icon="Cut" Label="切り取り" />
                    <AppBarButton Grid.Row="1" Icon="SelectAll" Label="すべて選択" />
                    <AppBarButton Grid.Row="1" Grid.Column="1" Icon="Copy" Label="コピー" />
                </Grid>
            </controls:RibbonCollapsibleGroup>

            <controls:RibbonCollapsibleGroup Label="ファイル"
                                             IconSource="{ui:FontIconSource Glyph=&#xE838;}"
                                             Style="{StaticResource RibbonLeftCollapsibleGroupStyle}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <AppBarButton Icon="{ui:FontIcon Glyph=&#xECC8;}" Label="新規作成" />
                    <AppBarButton Grid.Column="1" Icon="NewWindow" Label="新しいウィンドウ" />
                    <AppBarButton Grid.Row="1" Icon="{ui:FontIcon Glyph=&#xE838;}" Label="開く" />
                    <AppBarButton Grid.Row="1" Grid.Column="1" Icon="Save" Label="保存" />
                </Grid>
            </controls:RibbonCollapsibleGroup>
        </controls:Ribbon>
    </StackPanel>
</UserControl>
