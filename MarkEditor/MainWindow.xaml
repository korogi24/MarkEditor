<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="MarkEditor.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MarkEditor"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:win="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls"
    xmlns:ui="using:CommunityToolkit.WinUI"
    xmlns:tcb="using:MarkEditor"
    mc:Ignorable="d"
    Title="MarkEditor">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TitleBar Grid.Row="0" x:Name="TitleBar">
            <TitleBar.LeftHeader>
                <StackPanel Spacing="1" VerticalAlignment="Top" Orientation="Horizontal">
                    <BitmapIcon UriSource="ms-appx:///Assets/StoreLogo.png" Width="16" Height="16" Margin="15,0,10,0" />
                    <AppBarButton Icon="Save" Width="40" ToolTipService.ToolTip="上書き保存" />
                    <AppBarButton Icon="{ui:FontIcon Glyph=&#xE7A7;}" Width="40" ToolTipService.ToolTip="元に戻す" />
                </StackPanel>
            </TitleBar.LeftHeader>
            <TitleBar.Content>
                <AutoSuggestBox Width="450"
                                VerticalAlignment="Center"
                                CornerRadius="15"
                                PlaceholderText="検索"
                                QueryIcon="Find" />
            </TitleBar.Content>
        </TitleBar>

        <tcb:TabbedCommandBarControl Grid.Row="1" x:Name="TabbedCommandBar" />

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <WebView2 x:Name="Monaco" Grid.Column="0" />

            <WebView2 x:Name="Preview" Grid.Column="1" />
        </Grid>
    </Grid>
</Window>